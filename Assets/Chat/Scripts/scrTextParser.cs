using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scrTextParser : MonoBehaviour

{
    public GameObject chatManager;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
      //Check if enter is pressed, or if send message button is pressed
      //Call Submit
      
    }

    void Submit() {//pushes message to chat manager
        //get DateTime
        //get PlayerName
        //add message
        //Submit data to chatmanager
        
    }
}
